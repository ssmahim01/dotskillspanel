import TeamSalaryPageClient from "@/components/dashboard/team-salary/team-salary-page-client";

export const metadata = {
  title: "Team Salary Management | DotSkills Panel",
  description: "Manage monthly payroll, salary payments, and outstanding dues.",
};

export default function TeamSalaryPage() {
  return <TeamSalaryPageClient />;
}
